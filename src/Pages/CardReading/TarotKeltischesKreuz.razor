@page "/kartenlegen/tarot-keltisches-kreuz"
@using Toolbox.Models.CardReading
@using Toolbox.Resources
@inherits CardReadingPageBase

<PageTitle>@PageTitleText</PageTitle>

<DeckSelectionBar IsLoading="IsLoadingDecks"
                  DeckOptions="DeckOptions"
                  @bind-SelectedDeck="SelectedDeck"
                  ShowSearchButton="false"
                  SearchEnabled="IsSearchEnabled"
                  OnSearchRequested="HandleSearchRequested" />

<h1>@PageHeaderText</h1>

<TarotCardTable Spread="SpreadDefinition"
                ShowCards="@HasSelectedDeck"
                SelectedDeckId="@SelectedDeck"
                OnDeckLoadingChanged="HandleDeckLoadingChanged" />

@code {
    protected override string PageTitleText => DisplayTexts.CardReadingCelticCrossPageTitle;

    protected override string PageHeaderText => DisplayTexts.CardReadingCelticCrossPageHeader;

    protected override TarotSpreadLayout SpreadDefinition => TarotSpreadDefinitions.CelticCross;
}
